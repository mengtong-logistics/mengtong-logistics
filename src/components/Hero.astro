---
---
<section class="hero relative h-[100dvh] w-full overflow-hidden bg-primary">
  <!-- Background Image -->
  <img
    src="https://images.unsplash.com/photo-1601584115197-04ecc0da31d7?q=80&w=2070&auto=format&fit=crop"
    alt="货运车队"
    class="absolute inset-0 h-full w-full object-cover opacity-40 scale-105"
    loading="eager"
  />
  <div class="absolute inset-0 bg-gradient-to-t from-primary via-primary/60 to-transparent"></div>

  <!-- Content -->
  <div class="hero-content absolute bottom-0 left-0 flex h-full w-full flex-col justify-end p-8 pb-24 md:p-16 md:pb-32 max-w-7xl mx-auto">
    <div class="hero-badge mb-6 inline-block self-start rounded-full border border-white/20 bg-white/10 px-5 py-2 text-xs font-bold tracking-widest text-white backdrop-blur-md opacity-0 translate-y-12">
      扎根货运二十载 · 重信守诺 · 安全必达
    </div>
    <h1 class="hero-title text-5xl md:text-7xl lg:text-8xl font-black text-white leading-[1.1] tracking-tight opacity-0 translate-y-12">
      承载重托<br />
      <span class="font-serif italic text-gray-300 font-light tracking-normal">安全必达.</span>
    </h1>
    <p class="hero-desc mt-6 max-w-xl text-lg text-gray-400 font-medium opacity-0 translate-y-12">
      放弃花哨的互联网噱头，回归货运本质。我们以20年实体企业为底盘，为您提供不扯皮、负责到底的真实运力。
    </p>
    <div class="hero-actions mt-10 flex flex-wrap gap-4 opacity-0 translate-y-12">
      <a
        href="tel:13910225509"
        class="group relative overflow-hidden rounded-full bg-primary text-white px-8 py-4 font-bold tracking-wider shadow-xl shadow-blue-900/20 transition-all duration-200 hover:scale-[1.03] active:scale-[0.97]"
      >
        <span class="absolute inset-0 z-0 h-full w-full translate-y-full bg-accent transition-transform duration-500 ease-[cubic-bezier(0.25,0.46,0.45,0.94)] group-hover:translate-y-0"></span>
        <span class="relative z-10 flex items-center gap-2">
          拨打询价热线
          <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
        </span>
      </a>
      <a
        href="/#services"
        class="group relative overflow-hidden rounded-full bg-white text-primary border border-gray-200 px-8 py-4 font-bold tracking-wider shadow-sm transition-all duration-200 hover:scale-[1.03] hover:border-gray-300 active:scale-[0.97]"
      >
        <span class="absolute inset-0 z-0 h-full w-full translate-y-full bg-gray-100 transition-transform duration-500 ease-[cubic-bezier(0.25,0.46,0.45,0.94)] group-hover:translate-y-0"></span>
        <span class="relative z-10">了解业务</span>
      </a>
    </div>
  </div>
</section>

<script>
  // Staggered entrance animation
  document.addEventListener('DOMContentLoaded', () => {
    const items = [
      { el: '.hero-badge', delay: 200 },
      { el: '.hero-title', delay: 350 },
      { el: '.hero-desc', delay: 500 },
      { el: '.hero-actions', delay: 650 },
    ];
    items.forEach(({ el, delay }) => {
      const dom = document.querySelector(el) as HTMLElement;
      if (dom) {
        setTimeout(() => {
          dom.style.transition = 'all 1s cubic-bezier(0.16, 1, 0.3, 1)';
          dom.style.opacity = '1';
          dom.style.transform = 'translateY(0)';
        }, delay);
      }
    });
  });
</script>
